# Policy Drift Detection

Describes how divergence checks evaluate configuration discrepancies.

## 1. Comparing Config States
- Compares active tenant keys to identify missing or altered values.

## 2. Risk Classification
- Rates risk severity (e.g. `high` severity if limits parameters are updated).
